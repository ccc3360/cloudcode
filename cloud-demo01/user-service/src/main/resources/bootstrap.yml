spring:
  application:
    name: userservice
  profiles:
    active: dev #环境
  cloud:
    nacos:
#      server-addr: localhost:80
      server-addr: localhost:8848
      config:
        namespace: public  #指定命名空间
        file-extension: yaml #文件后缀名